
struct DetectRuleSets {
    include_status: Vec<CompactString>,
    min_level: CompactString,
    exclude_tags: Vec<CompactString>
}

pub fn rule_selctor() {
    let output_selector = vec![
        Rule
    ];
}